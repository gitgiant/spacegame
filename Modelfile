# Pull the base Gemma 4 model
FROM gemma4:26b

# Set the context window size to 64,000 tokens
PARAMETER num_ctx 64000

# Optional: Adjust thread performance for local execution
PARAMETER num_thread 8
